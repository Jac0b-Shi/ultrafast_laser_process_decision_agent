# 项目计划

## MVP 目标

面向超快激光加工场景，完成从实验数据整理、相似案例检索、参数推荐到反馈写回的可运行闭环。

## 阶段

1. 数据层：将多材料 Excel 原始数据整理为统一 schema。
2. 推荐层：基于材料过滤、目标质量和参数约束返回相似案例与推荐参数。
3. 反馈层：将实验结果追加写入系统，并纳入后续推荐。
4. 界面层：提供任务输入、推荐展示、数据概览和反馈录入工作台。

## 非目标

- 首版不控制真实激光设备。
- 首版不要求大模型在线可用。
- 首版不承诺全局最优，只提供可解释的参数初选和迭代依据。
