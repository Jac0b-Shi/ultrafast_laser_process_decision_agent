# 超快激光加工实验反馈日志（格式模板）
#
# 本文件展示 auto-feedback-logger.py 的输出格式，供开发与测试参考。
# 实际运行时日志由脚本自动生成，存放在 experiments/实验数据反馈日志/，
# 该目录已在 .gitignore 中排除，运行时日志不进仓库。
#
# ── 格式说明 ──────────────────────────────────────────────
#  第1段：元信息    — 日期、材料、目标
#  第2段：推荐参数  — 可包含 0~N 条推荐（回归模型 / 历史案例）
#  第3段：实测结果  — 含误差分析（深度偏差、粗糙度判定、预测偏差）
#  第4段：反馈录入  — 操作员、生成时间、备注
# ─────────────────────────────────────────────────────────

日期：<YYYY-MM-DD>
加工材料：<4H碳化硅 | BF33 | 微晶玻璃 | 金刚石 | 高温合金>
目标：深度<数值>μm，粗糙度≤<数值>μm

1. 系统推荐参数
回归模型推荐0：
得分：<0.0000~1.0000>
推荐参数：
脉冲宽度（fs）：<数值>
重复频率（kHz）：<数值>
扫描速度（mm/s）：<数值>
预测质量：
  预测深度：<数值> μm
  预测粗糙度：<数值> μm

历史案例推荐1：
得分：<0.0000~1.0000>
推荐参数：
脉冲宽度（fs）：<数值>
重复频率（kHz）：<数值>
扫描速度（mm/s）：<数值>
预测质量：
  预测深度：<数值> μm
  预测粗糙度：<数值> μm

2. 实测结果
选用推荐：<回归模型 | 历史案例><序号>（得分 <数值>）
加工深度：<数值> μm
表面粗糙度：<数值> μm

误差分析：
  深度偏差：<±数值> μm（<±数值>%）
  深度绝对误差：<数值> μm
  粗糙度余量：<±数值> μm（上限 <数值> μm）
  粗糙度判定：[OK] 达标  或  [X] 超标（+<数值> μm）
  预测深度偏差：<±数值> μm（<±数值>%）
  预测粗糙度偏差：<±数值> μm（<±数值>%）

3. 反馈录入
操作员：<姓名（可选）>
生成时间：<YYYY-MM-DD HH:MM:SS>
本日志由 scripts/auto-feedback-logger.py 自动生成
备注：<自由文本（可选）>
